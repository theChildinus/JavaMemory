1.进入./MyPython/volatility/plugins/linux
2.找到linue_memory_analyze.py 核心内容已经注释

其中，
“
    with open("./initial/objfile/uclientobj",'r') as f:   #set path  ***  虚拟机中正在运行的c语言程序的反汇编码文件

”

检查汇编文件是否和虚拟机中正在运行的程序一致，不一致重新反汇编，然后scp传到主机,放入MyPython/initial/objfile/中。
3.运行虚拟机中测试程序  uclient或者uclient1

4.进入MyPython 找到 exec.sh执行脚本  sh exec.sh


